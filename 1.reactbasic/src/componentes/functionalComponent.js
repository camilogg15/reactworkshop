import React, { Component } from 'react';
const Footer = ({fecha}) => {
   // const {fecha} = props;
    return (
        <footer>
            <h2>Camilo App {fecha}</h2>
        </footer>
    );
}

export default Footer;

